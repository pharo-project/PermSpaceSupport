Extension { #name : #Symbol }

{ #category : #'*PermSpace' }
Symbol >> moveToPermSpace [

	"Only move non special selectors"
"	(Smalltalk specialSelectors includes: self)
 		ifFalse: [ super moveToPermSpace ]"
]
